<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sherif.organiser">
<uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SabahMasaa"
            android:label="أذكار الصباح والمساء"/>
        <activity android:name=".AzkarActivity"
            android:label="أذكار ما بعد الصلوات"/>
        <activity android:name=".Grand"
            android:label="جدول المتابعة اليومية"/>
        <activity android:name=".TheResult"
            />
        <activity android:name=".Tesst"
            android:label="نتائج الجدول">

        </activity>
    </application>

</manifest>